<UserControl x:Class="Shadowsocks.Net.UI.WPF.ConfigModule.Views.ConfigListNode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Shadowsocks.Net.UI.WPF.ConfigModule.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <materialDesign:Card Grid.Row="0">
            <TextBlock Text="{Binding ServerConfig.Host}" Margin="16" TextAlignment="Center" />
        </materialDesign:Card>
        <Button Grid.Row="1" Style="{StaticResource MaterialDesignFloatingActionMiniButton}" Content="{materialDesign:PackIcon PencilOutline}" Command="{Binding OpenConfigDialogCommand}" Margin="0 -16 8 0" HorizontalAlignment="Right" Cursor="Hand" />
    </Grid>
</UserControl>